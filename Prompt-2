你是 SeaLantern 的 Code Agent。本轮你只负责插件运行时这一块，不要提交 commit，不要额外产出 markdown 文件，最后直接口述结果。

## 你的目标

把插件运行时里两个已经明确暴露的问题处理掉：前台进程超时是假的，权限元数据已经漂移。

## 已知问题

这些结论已经有前置审查，不要再把时间主要花在复现上，直接以修复为目标：

1. `sl.process.exec` 的前台超时是假的。当前实现会先 `wait_with_output()` 阻塞，等进程结束后才检查超时，所以长命令不会被及时中断。
2. manifest 校验层不承认 action 级 `fs.*` 权限，但运行时、文档、现有测试都已经支持它们。
3. `plugins` 与 `plugin_folder_access` 之间存在漂移：加载器会放行两者，但运行时只认 `plugin_folder_access`。

## 允许你修改的文件

- `src-tauri/src/plugins/runtime/**`
- `src-tauri/src/plugins/loader.rs`
- `src-tauri/tests/unit/plugins_runtime_*`
- `src-tauri/tests/unit/plugins_loader_tests.rs`

## 不要碰的文件

- `src-tauri/src/adapters/http/**`
- `src-tauri/src/runtime/**`
- `src-tauri/src/utils/cli/**`
- `src-tauri/src/services/server/**`

## 实现要求

1. 把 `sl.process.exec` 的前台执行改成真实超时控制。最低要求是到达 deadline 后主动 kill，并且 wait 回收子进程。
2. 不要只补表面报错，超时逻辑必须真实生效，避免插件用前台长命令卡住调用线程。
3. 权限定义要收敛成单一来源，至少不要再让加载器白名单和运行时能力各写一份互相漂移的规则。
4. action 级 `fs.*` 权限要能被合法声明并通过校验。
5. 你必须明确处理 `plugins` 和 `plugin_folder_access` 的关系。
   - 要么统一兼容并归一化。
   - 要么明确废弃其中一个，并让校验与运行时保持一致。

## 测试要求

至少补这些测试：

- `sl.process.exec` 长命令超时测试，断言 deadline 附近返回，并验证子进程已终止回收。
- manifest 权限校验测试，覆盖 `fs.data.read`、`fs.data.write`、`fs.server.list`、`fs.global.transfer` 等 action 级权限。
- `plugins` / `plugin_folder_access` 行为测试，确保“校验通过但运行时不可用”这种情况消失。
- 保留并复用现有 `plugins_runtime_filesystem_test.rs` 的回归语义，不要把已有细粒度权限能力做坏。

## 输出方式

结束时不要写文件总结，不要 commit，直接口述：

1. 你改了哪些文件。
2. 你对 `plugins` 和 `plugin_folder_access` 最终做了什么决定。
3. 你跑了什么测试，哪些没跑。
4. 还剩什么风险。

## 额外提醒

- 这轮每个 Agent 的记忆是清空的，所以不要假设别人会顺手帮你补权限定义；你需要在自己的边界里把这件事做完整。
- 不要顺手去改 HTTP、CLI 或服务端运行时文件。
