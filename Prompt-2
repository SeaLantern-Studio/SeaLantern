你是 SeaLantern 的 Code Agent。本轮你只负责插件运行时这一块的“真实验证 + 最小修补”，不要提交 commit，不要额外产出 markdown 文件，最后直接口述结果。

## 当前执行顺序

- 你这一轮属于第一批执行。
- 你要和 `Prompt-1` 对应的 Agent 并行工作。
- 在你和 `Prompt-1` 都结束前，不会开 `Prompt-3`。

## 你的目标

上轮插件运行时方向基本对，但验证链不可信。你这轮不要继续扩需求，重点是把已有改动用正确方式验证清楚，必要时只做最小修补。

## 已知背景

1. `sl.process.exec` 的前台超时逻辑已经被改动过。
2. `plugins -> plugin_folder_access` 的兼容归一化已经有实现痕迹。
3. Agent 5 已确认：上轮“测试没跑起来是因为全局编译坏了”这个解释不可信。
4. 上轮更可能的问题是测试过滤命令没有命中真实测试名，导致 0 tests。

## 允许你修改的文件

- `src-tauri/src/plugins/runtime/**`
- `src-tauri/src/plugins/loader.rs`
- `src-tauri/tests/unit/plugins_runtime_*`
- `src-tauri/tests/unit/plugins_loader_tests.rs`

## 不要碰的文件

- `src-tauri/src/adapters/http/**`
- `src-tauri/src/runtime/**`
- `src-tauri/src/utils/cli/**`
- `src-tauri/src/services/server/**`

## 这轮必须完成的事

1. 用正确可命中的测试方式，重新验证：
   - 前台 exec 真超时
   - action 级 `fs.*` 权限合法声明
   - `plugins` 和 `plugin_folder_access` 兼容行为
2. 如果验证中发现实现确实还有小问题，可以修，但不要顺手继续扩接口。
3. 不要再用“仓库当前全局不可编译”作为默认解释，除非你能给出这轮现有代码下真实可复现的报错。

## 测试要求

至少把这些点用真实测试命中确认：

- `test_process_exec_timeout_kills_and_reaps_foreground_process`
- `test_validate_manifest_allows_action_level_fs_permissions`
- `test_plugins_legacy_permission_alias_mounts_plugins_namespace`
- 现有细粒度 `fs.*` 行为回归测试至少抽一条确认不坏

## 输出方式

结束时不要写文件总结，不要 commit，直接口述：

1. 你改了哪些文件。
2. 你实际跑了哪些可命中的测试名。
3. 哪些测试通过，哪些失败。
4. `plugins` / `plugin_folder_access` 最终语义是什么。
5. 如果上轮的失败归因是错的，你要直接说明错在哪。

## 额外提醒

- 这轮核心不是“继续写更多代码”，而是把已有改动验证立住。
- 不要顺手碰 HTTP、CLI、server runtime。
