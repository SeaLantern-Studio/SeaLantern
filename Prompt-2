你是 Code Agent 2。

这轮任务只补“真实启动详情接口闭环”，不要碰前端页面，不要处理建服默认值。

调度说明：这是第一阶段任务，可与 `Prompt-1` 并行启动。

唯一输出要求：正常修改代码，并把结果写入 `.cache2`，不要口述回复，不要 commit。

必须遵守：
- 允许编辑 Rust 后端、必要的 Tauri command 注册、必要的 TS API 层。
- 不要编辑 `.vue` 页面组件。
- 不要改 schema 主结构，除非为了接口返回类型最小补充。
- 不要 commit。

必须阅读：
- `src-tauri/src/commands/server/manage.rs`
- `src-tauri/src/commands/server/manage/runtime.rs`
- `src-tauri/src/runtime/command_catalog.rs`
- `src-tauri/src/services/server/manager/runtime_start/local_launch_detail.rs`
- `src-tauri/src/services/server/runtime/docker_itzg/helpers.rs`
- `src/api/server.ts`
- `.cache2`
- `.cache3`

本轮你应该实现的范围：
1. 检查并补齐前端可直接调用的 API：
   - `get_local_launch_detail`
   - `get_docker_launch_detail`
2. 如果 TS API 层还没暴露对应方法，补上它们。
3. 返回类型要能让前端直接显示：
   - 启动方式
   - 启动命令概览
   - 实际 JVM 参数或可展示的参数概览
   - 预设应用状态
4. 补上最小验证，确保这两条接口可用。

本轮不要做：
- 不要改前端 UI。
- 不要改建服默认值返回。

完成后 `.cache2` 输出格式：
- `已修改文件`
- `完成内容`
- `前端现在可以直接调用什么`
- `验证结果`
- `遗留风险`
