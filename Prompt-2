你是 Code Agent 2。

这轮任务进入实现。你只负责“本地运行时 MVP”的代码修改，不要碰共享 schema 定义，不要碰 Docker 运行时，不要碰前端页面。

调度说明：这是第一阶段任务，可与 `Prompt-1` 并行启动。

唯一输出要求：正常修改代码，并把你的过程结果直接写入 `.cache2`，不要口述回复，不要 commit。

必须遵守：
- 允许编辑与你职责相关的 Rust 文件。
- 不要编辑 `src-tauri/src/models/server.rs` 里的结构定义，除非为编译对接必须最小引用；schema 主改动由 `Prompt-1` 负责。
- 不要编辑 Docker 运行时文件。
- 不要编辑任何前端文件。
- 不要 commit。

必须阅读：
- `.cache2`
- `.cache1`
- `src-tauri/src/services/server/manager/startup_support.rs`
- `src-tauri/src/services/server/manager/runtime_start/launch/command_builder.rs`
- `src-tauri/src/services/server/manager/runtime_start/launch/runner.rs`
- `src-tauri/src/services/server/runtime/local.rs`
- `src-tauri/src/services/server/manager/runtime_start/launch/context.rs`

本轮你应该实现的范围：
1. 只对 `jar` / `starter` 两种模式实现本地 CPU policy 支持。
2. 实现 `-XX:ActiveProcessorCount` 自动注入逻辑。
3. 实现用户显式参数冲突时跳过自动注入。
4. 在 `spawn` 后按 PID 应用本地 affinity。
5. 为前端和 inspect 准备“本地启动详情”所需的后端信息，至少要能稳定拿到：
   - 启动方式
   - Java 路径
   - 启动目标
   - 实际会带上的 JVM 参数列表
   - 可展示的启动命令概览
6. 补本地日志与测试。

本轮不要做：
- 不要给 `bat/sh/ps1/custom` 加任何 CPU policy 支持。
- 不要改 Docker 逻辑。
- 不要改前端。
- 不要扩写 schema 结构定义。

如果因为 `Prompt-1` 还没落 schema 导致你无法编译，可以先按最小 stub 方式完成本地逻辑改造，并在 `.cache2` 里明确写出需要 `Prompt-1` 对接的编译点。

完成后 `.cache2` 输出格式：
- `已修改文件`
- `完成内容`
- `启动详情对外提供了什么`
- `需要 schema 对接的点`
- `遗留风险`
