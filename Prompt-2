你是 Code Agent 2。

这轮任务只做本地运行时的补丁级实现方案，不做代码实现。你唯一允许写入的文件是仓库根目录下的 `.cache2`。

目标：基于上一轮分析，产出一份可以直接交给后续实现 Agent 的本地运行时改造方案，精确到函数、分支、日志、测试点。

必须遵守：
- 不要编辑除 `.cache2` 之外的任何文件。
- 不要 commit。
- 不要口述回复，结果直接写入 `.cache2`。
- 不要再重复宏观讨论，直接给补丁级方案。

必须阅读：
- `.cache1`
- `.cache2`
- `docs/server-cpu-affinity-jvm-flags-draft.md`
- `src-tauri/src/services/server/manager/startup_support.rs`
- `src-tauri/src/services/server/manager/runtime_start/launch/command_builder.rs`
- `src-tauri/src/services/server/manager/runtime_start/launch/runner.rs`
- `src-tauri/src/services/server/runtime/local.rs`
- `src-tauri/src/models/server.rs`

你要输出到 `.cache2` 的内容：
1. 本地 MVP 支持边界最终建议，直接拍板哪些模式支持，哪些不支持。
2. `ActiveProcessorCount` 的精确注入位置、合成顺序、冲突跳过逻辑。
3. `CPU affinity` 的精确接入位置，按 Windows / Linux 分开说明。
4. 需要改动的文件列表，每个文件做什么。
5. 建议新增的辅助函数签名或模块边界。
6. 测试与日志补点清单。

输出格式：
- 用中文。
- 按下面顺序输出：
  1. `本地 MVP 拍板`
  2. `代码改动列表`
  3. `关键流程伪代码`
  4. `日志与错误处理`
  5. `测试点`
- 如果你认为某个模式首期不该支持，要直接写成排除项。
