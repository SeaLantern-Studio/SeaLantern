[package]
name = "sealantern-core"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"

[lib]
path = "src/lib.rs"

[dependencies]
tracing = "0.1"
zip = { version = "2.0", default-features = false, features = ["deflate"] }
