//! 服务器实例资源（模组 / 插件 / 数据包）管理。
//!
//! 分为两个子域：
//! - [`market`]：从外部平台（Modrinth / Spiget）获取资源数据与文件；
//! - [`manager`]：管理实例目录中的资源文件（扫描 / 增删 / 启用禁用 / 清单）。
//!
//! 本模块只处理"实例目录里的资源文件"，不承载开服核心逻辑
//! （进程管理、服务端检测、供给规划）。

pub mod manager;
pub mod market;
