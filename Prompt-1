你是 SeaLantern 的 Code Agent。本轮你只负责一块明确边界内的实现，不要提交 commit，不要额外产出 markdown 文件，最后直接口述你做了什么、验证了什么、还有什么风险。

## 你的目标

把 headless HTTP 的暴露面和上传链路一起收口，原因很简单：这两块都集中在 `src-tauri/src/adapters/http/server.rs`，拆给两个 Agent 会直接撞文件。

## 你要处理的问题

当前已知问题如下，这些都是已经审出来的，不要重复花太多时间重新证明，直接基于它们落修复：

1. headless HTTP 默认外网暴露，且没有鉴权边界。
2. CORS 现在是 `Any`，浏览器跨域面过宽。
3. 上传实现还是整文件读入内存，`500 MiB` 的 body limit 不能防止容器内存被打满。
4. 上传文件名没有做 basename 归一化，也没有对最终目标路径做目录边界校验。
5. multipart 解析错误和字段读取错误会被伪装成“部分成功”。
6. 目标文件名只靠秒级时间戳去重，存在同秒覆盖风险。

## 允许你修改的文件

- `src-tauri/src/runtime/mode.rs`
- `src-tauri/src/runtime/mod.rs`
- `src-tauri/src/adapters/http/mod.rs`
- `src-tauri/src/adapters/http/server.rs`
- 你为这条链路新增的测试文件

## 不要碰的文件

- `src-tauri/src/adapters/http/command_registry/*`
- `src-tauri/src/plugins/**`
- `src-tauri/src/utils/cli/**`
- `src-tauri/src/services/server/**`

## 实现要求

1. 默认绑定地址不要再是 `0.0.0.0:3000`，应收紧为默认本地可达，只有显式配置时才允许外网监听。
2. 给 `/api/*`、`/upload`、`/api/logs/stream` 统一加鉴权；`/health` 可以匿名访问。
3. CORS 改成默认关闭跨域，只有显式白名单时才放行；不要继续 `allow_origin(Any)`。
4. 上传改为流式写盘，不要再用整文件聚合到内存的方式。
5. 上传路径必须使用安全 basename，并验证最终目标仍位于上传目录内。
6. 上传错误必须明确返回失败，不能出现“部分成功但状态码还是 200”。
7. 文件去重不要只依赖秒级时间戳，改成更稳的唯一名策略。

## 测试要求

至少补这些测试，能自动化就自动化：

- 无 token 访问 `/api/*`、`/upload`、`/api/logs/stream` 必须失败。
- `/health` 无 token 仍可访问。
- 默认绑定策略应收紧，本地默认可达，外网绑定需要显式配置。
- 文件名安全测试：`../x.jar`、`a/../b.jar`、`a\\b.jar`、空名、控制字符、平台保留名。
- 目录边界测试：无论输入文件名如何变化，最终保存路径都必须留在上传目录内。
- 大文件 / 多文件限制测试。
- multipart 解析失败和字段读取失败必须返回失败状态。
- 同秒同名上传不能互相覆盖。

## 输出方式

结束时不要写文件总结，不要 commit，直接口述：

1. 你改了哪些文件。
2. 你怎么验证的。
3. 还有哪些风险没处理。

## 额外提醒

- 这一轮别和其他 Agent 抢文件，尤其不要改 `command_registry/*`。
- 如果你发现现有结构不适合一次性把鉴权和上传都做完，可以优先保证安全边界先落地，但最后必须说明取舍。
