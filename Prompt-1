你是 Code Agent 1。

这轮任务只做“现行可达入口复核”。目标不是继续扩实现，而是把现在用户真实能走到的创建/接入路径再扫一遍，确认 `cpu_policy + jvm_preset + jvm_args` 有没有漏传、漏校验、桌面和浏览器桥层不一致的地方。

调度说明：这是第一阶段任务，可与 `Prompt-2` 并行启动。

唯一输出要求：正常修改代码，并把结果写入 `.cache1`，不要口述回复，不要 commit。

必须遵守：
- 允许编辑前端 TS、必要的 API 对接、必要的桥层请求结构。
- 不要改运行时读取优先级。
- 不要重做启动详情展示卡片。
- 不要开新表单。
- 不要 commit。

必须阅读：
- `src/views/CreateServerView.vue`
- `src/components/views/create/useCreateServerPage.ts`
- `src/components/views/create/useCreateServerSubmit.ts`
- `src/components/views/create/useCreateServerDefaults.ts`
- `src/api/server.ts`
- `src-tauri/src/commands/server/manage.rs`
- `src-tauri/src/commands/server/manage/provisioning.rs`
- `src-tauri/src/adapters/http/command_registry/requests.rs`
- `src-tauri/src/adapters/http/command_registry/server.rs`
- `.cache1`

本轮你应该实现的范围：
1. 只盯“当前前端页面可真实到达”的建服/接入路径，逐条核对：
   - 页面状态里有没有带上 `jvm_args`
   - 有没有带上 `cpu_policy`
   - 有没有带上 `jvm_preset`
   - 提交前有没有统一 CPU 策略校验
2. 同时核对桌面 Tauri 调用和浏览器 HTTP 命令桥的字段面是否一致。
3. 如果发现某条现行可达路径还缺字段，就补到那一条；不要去扩大到仓库里没有页面入口的旧 API。
4. 如果确认某些 API 已补齐但页面并不可达，要明确写进 `.cache1`，不要算成“用户已可用”。
5. 补最小验证，至少说明现行可达入口最后落到哪条 API/命令链。

本轮不要做：
- 不要改后端运行时合成逻辑。
- 不要改设置页大结构。
- 不要顺手增加新能力。

完成后 `.cache1` 输出格式：
- `已修改文件`
- `现行可达入口`
- `每条入口最终落点`
- `补掉的问题`
- `验证结果`
- `遗留风险`
