你是 SeaLantern 的 Code Agent。本轮你只负责 HTTP 这一条链路的收尾和校准，不要提交 commit，不要额外产出 markdown 文件，最后直接口述结果。

## 当前执行顺序

- 你这一轮属于第一批执行。
- 你要和 `Prompt-2` 对应的 Agent 并行工作。
- 在你和 `Prompt-2` 都结束前，不会开 `Prompt-3`。

## 你的目标

这轮不要再重做整条 HTTP 链路，而是只处理上轮留下的两个明确尾巴，并把验证说实：

1. 未配置静态 token 时，启动日志里会打印完整 token，这会削弱鉴权收益。
2. 上传链路虽然已经有总 body limit 和文件数限制，但还缺少明确的单文件大小上限语义。

## 允许你修改的文件

- `src-tauri/src/runtime/mode.rs`
- `src-tauri/src/runtime/mod.rs`
- `src-tauri/src/adapters/http/mod.rs`
- `src-tauri/src/adapters/http/server.rs`
- 这条链路对应的测试文件

## 不要碰的文件

- `src-tauri/src/adapters/http/command_registry/*`
- `src-tauri/src/plugins/**`
- `src-tauri/src/utils/cli/**`
- `src-tauri/src/services/server/**`

## 这轮必须完成的事

1. 不要再把完整 bearer token 直接打到长期日志里。
   - 可以输出指纹、前缀、使用提示。
   - 但不要把完整敏感值直接暴露出去。
2. 给上传链路补上“单文件大小上限”语义，而不是只靠总 body limit。
3. 重新跑真实可命中的 HTTP 定向测试，不要只说“跑过了”。
4. 如果你发现 Agent 5 校准结论和当前代码不一致，以当前代码为准，并在口述里说明。

## 测试要求

至少明确验证这些点：

- `/health` 匿名访问仍然成立。
- 受保护路由无 token 失败。
- token 日志不再泄露完整值。
- 单文件大小超限时明确失败。
- 文件数限制仍然有效。
- CORS 默认关闭、显式白名单可用。

## 输出方式

结束时不要写文件总结，不要 commit，直接口述：

1. 你改了哪些文件。
2. 你补了哪些测试，实际跑了哪些测试。
3. token 日志现在如何处理。
4. 单文件大小上限最终怎么定的。
5. 还剩什么风险。

## 额外提醒

- 不要继续扩张到 command registry 分级。
- 这轮是收尾和验真，不是再开新战线。
