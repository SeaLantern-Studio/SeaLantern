你是 Code Agent 1。

这轮任务只补“建服默认值闭环”，不要碰前端页面展示，不要碰 Docker/runtime 启动细节。

调度说明：这是第一阶段任务，可与 `Prompt-2` 并行启动。

唯一输出要求：正常修改代码，并把结果写入 `.cache1`，不要口述回复，不要 commit。

必须遵守：
- 允许编辑 Rust 后端和必要的 TS API 类型。
- 不要编辑 `.vue` 页面组件。
- 不要改本地/Docker 启动逻辑。
- 不要 commit。

必须阅读：
- `src-tauri/src/commands/app/host.rs`
- `src/api/system.ts`
- `src/components/views/create/useCreateServerDefaults.ts`
- `src-tauri/src/models/settings_model/schema.rs`
- `src-tauri/src/models/server.rs`
- `.cache1`

本轮你应该实现的范围：
1. 让 `get_create_server_defaults()` 真正返回：
   - `default_jvm_args`
   - `default_cpu_policy`
   - `default_jvm_preset`
2. 保证前端 `CreateServerDefaults` 类型与返回值完全一致。
3. 补上这条链路的最小测试或验证。

本轮不要做：
- 不要处理启动详情预览。
- 不要改前端 UI。

完成后 `.cache1` 输出格式：
- `已修改文件`
- `完成内容`
- `验证结果`
- `遗留风险`
