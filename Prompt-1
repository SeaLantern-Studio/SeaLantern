你是 Code Agent 1。

这轮任务先处理“实例级启动配置文件落点”，把可编辑项稳定放到服务器目录内，前端页面先不要碰。

调度说明：这是第一阶段任务，可与 `Prompt-2` 并行启动。

唯一输出要求：正常修改代码，并把结果写入 `.cache1`，不要口述回复，不要 commit。

必须遵守：
- 允许编辑 Rust 后端、必要的 Tauri command、必要的 TS API 类型。
- 不要编辑 `.vue` 页面组件。
- 不要改启动详情页面文案。
- 不要 commit。

必须阅读：
- `src-tauri/src/commands/server/config.rs`
- `src/api/config.ts`
- `src/components/config/useStartupConfigSection.ts`
- `src-tauri/src/services/server/manager/provisioning/shared.rs`
- `src-tauri/src/commands/server/manage/provisioning.rs`
- `.cache1`

本轮你应该实现的范围：
1. 把服务器实例级启动配置从当前 `服务器目录/SL.json` 收敛到 `服务器目录/SeaLantern/config.toml`。
2. 配置内容至少覆盖：
   - `max_memory`
   - `min_memory`
   - `jvm_args`
   - `cpu_policy`
   - `jvm_preset`
3. 读取逻辑要兼容旧的 `SL.json`：
   - 已有旧文件时能继续读到
   - 写回时统一落到新的实例配置文件
4. 路径必须保证仍然限制在当前服务器目录内。
5. 补最小测试，覆盖新文件读写和旧文件兼容。

本轮不要做：
- 不要改 `.vue` 页面。
- 不要处理真实启动详情展示。
- 不要改 CPU 亲和性表单。

完成后 `.cache1` 输出格式：
- `已修改文件`
- `实例配置现在落在哪`
- `旧配置兼容情况`
- `验证结果`
- `遗留风险`
