# 下一轮分发顺序

这轮只补闭环，不扩范围。

## 第一阶段

并行启动：

- `Prompt-1`
- `Prompt-2`

目标：

- 把建服默认值返回补完整
- 把真实启动详情接口对前端开放完整

## 第二阶段

在 `.cache1` 和 `.cache2` 回来后，再启动：

- `Prompt-3`

目标：

- 让前端真正用上这些返回值和启动详情接口
- 把“概览”和“真实启动详情”的边界讲清楚

## 说明

- 这轮不再拆 4 个实现方向。
- 至少保留 2 个并行 Agent，先补最明显的断口。
