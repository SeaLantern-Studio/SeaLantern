# 下一轮分发顺序

这轮先把实例配置落点和展示断口收掉，再补 CPU 亲和性表单。

## 第一阶段

并行启动：

- `Prompt-1`
- `Prompt-2`

目标：

- 把实例级启动配置稳定落到 `服务器目录/SeaLantern/config.toml`
- 保留对旧 `SL.json` 的读取兼容
- 让配置页真正显示真实启动详情，而不是前端近似拼接

## 第二阶段

在 `.cache1` 和 `.cache2` 回来后，再启动：

- `Prompt-3`

目标：

- 把 CPU 亲和性补成可编辑项
- 接通配置页、建服页、默认设置页三条保存链路

## 说明

- 这轮至少保留 2 个并行 Agent。
- `Prompt-1` 和 `Prompt-2` 不要编辑同一份 `.vue` 文件。
- `.cacheN` 只作为回传，不要 commit。
