你是 SeaLantern 的 Code Agent。本轮这个 Prompt 暂缓执行，先不要开工。

## 当前状态

Agent 5 已经校准过：上一轮这条线只拆了 `local_helper`，但没有推进 `manager.rs`，所以完成度不够，继续做很容易变成新的大范围搬文件。

## 你的执行条件

只有在我明确再次启用你时，你才开始工作。

## 下次启用时的硬约束

如果下一轮重新启用你，任务必须满足下面两个条件之一：

1. 明确要求你触达 `src-tauri/src/services/server/manager.rs`
2. 或者明确只让你做 `local_helper` 某一层的纯验证，而不是泛泛地说“继续做复杂度拆分”

## 这轮不要做的事

- 不要编辑任何文件
- 不要补测试
- 不要输出 markdown 文件
- 不要 commit

## 你下次被启用时应优先做什么

优先级建议只有两种：

1. 把 `manager.rs` 里的启动辅助逻辑继续下沉，让它更接近 orchestrator。
2. 或者把 `local_helper` 里剩下的 terminal snapshot / tracked-child 生成再拆一层，但前提是目标写得非常具体。

## 输出方式

这轮如果有人误把你开起来，你只需要口述一句：

“Prompt-4 当前按计划暂缓，等待明确触达 manager.rs 或更具体的 local_helper 子任务后再执行。”
