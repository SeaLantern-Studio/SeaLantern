你是 Code Agent 4。

这轮任务进入实现。你只负责“前端 MVP 页面与交互”的代码修改，不要碰 Rust 运行时实现。

调度说明：这是第二阶段任务。请在第一阶段的 `.cache1` 完成后再启动。你可以与 `Prompt-3` 并行启动。

唯一输出要求：正常修改代码，并把你的过程结果直接写入 `.cache4`，不要口述回复，不要 commit。

必须遵守：
- 允许编辑前端类型、API、组件、语言包。
- 不要编辑 Docker 运行时 Rust 文件。
- 不要编辑本地运行时 Rust 文件。
- 不要 commit。

必须阅读：
- `.cache1`
- `.cache4`
- `src/types/server.ts`
- `src/api/server.ts`
- `src/api/config.ts`
- `src/api/settings.ts`
- `src/components/views/create/ServerStartupConfigStep.vue`
- `src/components/config/ConfigStartupSection.vue`
- `src/components/views/settings/ServerDefaultsCard.vue`
- `src/language/zh-CN.json`

本轮你应该实现的范围：
1. 按拍板合同补齐 TS 类型和 API 调用参数。
2. 建服页新增 `jvm_preset` 与 `jvm_args` 的 MVP 交互。
3. 编辑页新增 `jvm_preset` 与 `jvm_args` 的 MVP 交互。
4. 默认设置页新增 `default_jvm_preset`，并把 `default_jvm_args` 切到数组语义的文本视图。
5. 在合适的位置把“当前会怎样启动”做成可见信息，至少让用户看得到：
   - 启动方式
   - 启动命令或参数概览
   - 选中的 JVM 预设
   - 附加 JVM 参数
   - 预设应用后会带上的关键参数概览
6. CPU policy 这轮只入模、可回显、可透传，不上 UI 控件。
7. 补必要 i18n。

本轮不要做：
- 不要渲染 CPU policy 表单。
- 不要实现 Rust 端运行时逻辑。
- 不要在界面里使用工程化用语，例如 `DTO`、`argv`、`序列化`、`运行时抽象`。
- 不要使用命令式提示语或多余说明文字，文案只保留用户真正需要知道的内容。

完成后 `.cache4` 输出格式：
- `已修改文件`
- `完成内容`
- `启动信息呈现放在了哪里`
- `与后端合同对接情况`
- `遗留风险`
