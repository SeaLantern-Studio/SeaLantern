name: Publish to AUR

on:
  push:
    tags:
      - "v*" # 匹配 v0.7.3, v1.0.0 等
      - "sea-lantern-v*" # 匹配 sea-lantern-v0.7.3
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (e.g., 1.0.0)"
        required: true
        type: string
      skip_test:
        description: "Skip makepkg test"
        required: false
        default: false
        type: boolean

jobs:
  aur-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential fakeroot git curl wget

      - name: Debug GitHub context
        run: |
          echo "GitHub Context Debug:"
          echo "Event name: ${{ github.event_name }}"
          echo "Ref: ${{ github.ref }}"
          echo "Ref type: ${{ github.ref_type }}"
          echo "Ref name: ${{ github.ref_name }}"
          echo "Release tag: ${{ github.event.release.tag_name }}"
          echo "Repository: ${{ github.repository }}"
          echo "Actor: ${{ github.actor }}"

      - name: Get release version
        id: get_version
        run: |
          # 根据事件类型获取版本
          if [[ "${{ github.event_name }}" == "release" ]]; then
            # Release 事件 - 从 release tag 获取
            RAW_VERSION="${{ github.event.release.tag_name }}"
            echo "Source: release tag: $RAW_VERSION"
          elif [[ "${{ github.event_name }}" == "push" ]]; then
            # Push 事件（tag 推送）- 从 ref 获取
            RAW_VERSION="${{ github.ref_name }}"
            echo "Source: git tag: $RAW_VERSION"
          else
            # 手动触发 - 从输入获取
            RAW_VERSION="${{ github.event.inputs.version }}"
            echo "Source: manual input: $RAW_VERSION"
          fi

          # 提取纯净版本号（支持 sea-lantern-v0.6.2 或 v0.6.2 格式）
          if [[ $RAW_VERSION =~ [0-9]+\.[0-9]+\.[0-9]+ ]]; then
            # 使用正则提取数字版本号
            VERSION=$(echo "$RAW_VERSION" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
            echo "Extracted version from tag: $VERSION"
          else
            # 如果没有匹配到，尝试移除 'v' 前缀
            VERSION="${RAW_VERSION#v}"
          fi

          # 验证版本号不为空
          if [[ -z "$VERSION" ]]; then
            echo "❌ Error: Version is empty!"
            echo "Debug:"
            echo "  event_name: ${{ github.event_name }}"
            echo "  raw_version: $RAW_VERSION"
            exit 1
          fi

          # 验证版本号格式
          if ! [[ $VERSION =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "❌ Error: Invalid version format: $VERSION"
            echo "Version must be in format: x.y.z"
            exit 1
          fi

          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_ENV
          echo "✅ Final version: $VERSION"

      - name: Validate version
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"
          echo "Validating version: $VERSION"

          # 检查 PKGBUILD 中的版本号格式
          if grep -q "^pkgver=" PKGBUILD; then
            CURRENT_PKGVER=$(grep "^pkgver=" PKGBUILD | cut -d= -f2 | xargs)
            echo "Current PKGBUILD version: $CURRENT_PKGVER"
          fi

      - name: Debug version info
        run: |
          echo "Debug information:"
          echo "Event name: ${{ github.event_name }}"
          echo "Version from inputs: ${{ github.event.inputs.version }}"
          echo "Version from get_version output: ${{ steps.get_version.outputs.VERSION }}"
          echo "Version from env: ${{ env.version }}"
          echo "GITHUB_REF: ${{ github.ref }}"
          echo "GITHUB_REF_NAME: ${{ github.ref_name }}"

      - name: Validate PKGBUILD exists
        run: |
          if [ ! -f "PKGBUILD" ]; then
            echo "Error: PKGBUILD file not found!"
            echo "Current directory contents:"
            ls -la
            exit 1
          fi
          echo "✅ PKGBUILD found:"
          head -5 PKGBUILD

      - name: Update PKGBUILD version
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"

          if [[ -z "$VERSION" ]]; then
            echo "Error: VERSION is empty"
            exit 1
          fi

          echo "Updating PKGBUILD to version: $VERSION"

          # 备份原始 PKGBUILD
          cp PKGBUILD PKGBUILD.bak

          # 更新版本号
          sed -i "s/^pkgver=.*/pkgver=${VERSION}/" PKGBUILD
          sed -i "s/^pkgrel=.*/pkgrel=1/" PKGBUILD

          echo "✅ Updated PKGBUILD"
          echo "New version: $(grep ^pkgver= PKGBUILD)"
          echo "New release: $(grep ^pkgrel= PKGBUILD)"

      - name: Generate .SRCINFO using makepkg
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"
          echo "Generating .SRCINFO for version: $VERSION using makepkg"

          # 使用 Docker 运行 makepkg 生成 .SRCINFO
          docker run --rm -v $PWD:/workspace -w /workspace archlinux:latest bash -c "
            pacman -Sy --noconfirm base-devel && \
            makepkg --printsrcinfo > .SRCINFO
          "

          echo "✅ Generated .SRCINFO:"
          cat .SRCINFO

      - name: Validate files
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"

          echo "Validating files for version: $VERSION"

          # 验证 PKGBUILD
          if [ ! -f "PKGBUILD" ]; then
            echo "❌ PKGBUILD not found!"
            exit 1
          fi

          PKGVER=$(grep "^pkgver=" PKGBUILD | cut -d= -f2 | xargs)
          if [[ "$PKGVER" != "$VERSION" ]]; then
            echo "❌ PKGBUILD version mismatch!"
            echo "  Expected: $VERSION"
            echo "  Found: $PKGVER"
            exit 1
          fi
          echo "✅ PKGBUILD version matches: $PKGVER"

          # 验证 .SRCINFO
          if [ ! -f ".SRCINFO" ]; then
            echo "❌ .SRCINFO not found!"
            exit 1
          fi

          SRCINFO_VER=$(grep "^[[:space:]]*pkgver =" .SRCINFO | head -1 | awk '{print $3}')
          if [[ "$SRCINFO_VER" != "$VERSION" ]]; then
            echo "❌ .SRCINFO version mismatch!"
            echo "  Expected: $VERSION"
            echo "  Found: $SRCINFO_VER"
            exit 1
          fi
          echo "✅ .SRCINFO version matches: $SRCINFO_VER"

          # 显示文件内容以便调试
          echo ""
          echo "PKGBUILD content (first 10 lines):"
          head -10 PKGBUILD
          echo ""
          echo ".SRCINFO content:"
          cat .SRCINFO

      - name: Publish AUR package
        uses: KSXGitHub/github-actions-deploy-aur@v3.0.1
        with:
          pkgname: sealantern
          pkgbuild: PKGBUILD
          assets: |
            .SRCINFO
            sealantern.install
          commit_username: "${{ github.actor }}"
          commit_email: "${{ github.actor }}@users.noreply.github.com"
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          commit_message: "Update sealantern to v${{ steps.get_version.outputs.VERSION }}"
          allow_empty_commits: false
          force_push: false
          updpkgsums: false
          test: ${{ !github.event.inputs.skip_test }}
          ssh_keyscan_types: rsa,ecdsa,ed25519

      - name: Send success notification
        if: success()
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"
          echo "======================================"
          echo "✅ Successfully published sealantern v${VERSION} to AUR"
          echo "📦 Package URL: https://aur.archlinux.org/packages/sealantern"
          echo "======================================"

      - name: Send failure notification
        if: failure()
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"
          echo "======================================"
          echo "❌ Failed to publish sealantern v${VERSION} to AUR"
          echo "Please check the workflow logs for details."
          echo "======================================"

      - name: Cleanup
        if: always()
        run: |
          if [ -f "PKGBUILD.bak" ]; then
            echo "Restoring original PKGBUILD"
            mv PKGBUILD.bak PKGBUILD
          fi
