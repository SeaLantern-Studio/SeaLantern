你是 Code Agent 3。

这轮任务是第二阶段收口，只做复核和最小补洞。请在 `.cache1` 和 `.cache2` 回来后再启动，不要抢前两条线的文件。

调度说明：这是第二阶段任务。

唯一输出要求：正常修改代码，并把结果写入 `.cache3`，不要口述回复，不要 commit。

必须遵守：
- 允许做小范围修补和验证。
- 不要重做大结构，不要开新能力。
- 优先处理“实例配置写了但真实启动详情/实际启动没同步”的残留问题。
- 不要 commit。

必须阅读：
- `.cache1`
- `.cache2`
- `src/components/config/useStartupConfigSection.ts`
- `src/components/config/ConfigStartupSection.vue`
- `src-tauri/src/services/server/manager/startup_support.rs`
- `src-tauri/src/services/server/runtime/docker_itzg/helpers.rs`
- `src/components/views/create/useCreateServerSubmit.ts`

本轮你应该实现的范围：
1. 复核保存实例配置后，配置页重新拉取的真实启动详情是否已经能反映最新值。
2. 如果前两条线留下了字段映射、保存后刷新、空态提示之类的小断口，只做最小补齐。
3. 优先补测试或验证，再补代码；确实有断口再改。
4. 只处理和本轮目标直接相关的问题，不顺手扩范围。

本轮不要做：
- 不要新增新表单。
- 不要再迁移配置文件。
- 不要碰无关页面。

完成后 `.cache3` 输出格式：
- `已修改文件`
- `复核结论`
- `补掉了哪些小断口`
- `验证结果`
- `遗留风险`
