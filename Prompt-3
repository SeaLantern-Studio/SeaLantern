你是 Code Agent 3。

这轮任务只做 Docker `itzg` 运行时的补丁级实现方案，不做代码实现。你唯一允许写入的文件是仓库根目录下的 `.cache3`。

目标：基于上一轮分析，产出一份可以直接交给后续实现 Agent 的 Docker 运行时改造方案，精确到 `docker run` 参数层、JVM env 合成层、冲突处理与测试点。

必须遵守：
- 不要编辑除 `.cache3` 之外的任何文件。
- 不要 commit。
- 不要口述回复，结果直接写入 `.cache3`。
- 不要再给多个平行方案，这轮只给推荐落地方案。

必须阅读：
- `.cache1`
- `.cache3`
- `docs/server-cpu-affinity-jvm-flags-draft.md`
- `docs/docker-runtime-itzg-design.md`
- `src-tauri/src/utils/cli/server_docker.rs`
- `src-tauri/src/services/server/runtime/docker_itzg.rs`
- `src-tauri/src/services/server/runtime/docker_itzg/helpers.rs`
- `src-tauri/src/models/server.rs`

你要输出到 `.cache3` 的内容：
1. Docker MVP 最终拍板，`count` / `explicit` / `off` 分别怎么映射。
2. 明确这轮是否不做 `--cpus`，以及原因。
3. `JVM preset` 和 `ActiveProcessorCount` 的 env 合成策略，精确到推荐的 env key 和冲突规则。
4. 需要改动的文件列表，每个文件做什么。
5. Compose / inspect / status 层面是否需要同步考虑，哪些可以暂缓。
6. 测试与日志补点清单。

输出格式：
- 用中文。
- 按下面顺序输出：
  1. `Docker MVP 拍板`
  2. `参数映射与冲突规则`
  3. `代码改动列表`
  4. `关键流程伪代码`
  5. `测试点`
- 重点说明与本地运行时故意保持一致和故意不一致的地方。
