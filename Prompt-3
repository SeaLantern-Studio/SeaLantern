你是 Code Agent 3。

这轮任务是第二阶段的薄复查。请在 `.cache1` 和 `.cache2` 回来后再启动，只处理前两条线之间可能出现的冲突、遗漏或过界修改。

调度说明：这是第二阶段任务。

唯一输出要求：正常修改代码，并把结果写入 `.cache3`，不要口述回复，不要 commit。

必须遵守：
- 只做小范围复查和最小修补。
- 不要再扩新功能。
- 如果没有必要修改代码，就只写清楚复查结论。
- 不要 commit。

必须阅读：
- `.cache1`
- `.cache2`
- `git status --short`
- `src/components/config/useStartupConfigSection.ts`
- `src/components/views/create/useCreateServerSubmit.ts`
- `src-tauri/src/services/server/manager/startup_support.rs`
- `src-tauri/src/adapters/http/command_registry/requests.rs`

本轮你应该实现的范围：
1. 复查前两条线有没有改到同一条逻辑却留下不一致口径。
2. 复查“现行可达入口”和“建议 commit 分组”之间有没有遗漏的真实文件归属问题。
3. 只补最小冲突，不做额外实现。
4. 如果确认已经可以进入分类提交阶段，要明确写出来。

本轮不要做：
- 不要新增功能。
- 不要重写测试策略。
- 不要碰无关页面。

完成后 `.cache3` 输出格式：
- `已修改文件`
- `复查结论`
- `补掉的问题`
- `是否可以进入分类提交`
- `遗留风险`
