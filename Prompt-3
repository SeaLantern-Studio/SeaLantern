你是 Code Agent 3。

这轮任务进入实现。你只负责“Docker 运行时 MVP”的代码修改，不要碰前端页面，不要碰本地运行时实现。

调度说明：这是第二阶段任务。请在第一阶段的 `.cache1` 完成后再启动。你可以与 `Prompt-4` 并行启动。

唯一输出要求：正常修改代码，并把你的过程结果直接写入 `.cache3`，不要口述回复，不要 commit。

必须遵守：
- 允许编辑 Docker 运行时、compose、inspect 相关文件。
- 不要编辑前端页面文件。
- 不要编辑本地运行时文件。
- 不要 commit。

必须阅读：
- `.cache1`
- `.cache3`
- `src-tauri/src/services/server/runtime/docker_itzg.rs`
- `src-tauri/src/services/server/runtime/docker_itzg/helpers.rs`
- `src-tauri/src/utils/cli/compose_cli.rs`
- `src-tauri/src/utils/cli/server_manage_render.rs`
- `src-tauri/src/utils/cli/server_setup/docker_request_builder.rs`
- `src-tauri/src/models/server.rs`

本轮你应该实现的范围：
1. 实现 Docker `cpu_policy` 到 `--cpuset-cpus` 的映射，仅做 `off / count / explicit`。
2. 明确不做 `--cpus`。
3. 实现 Docker `jvm_preset + jvm_args + ActiveProcessorCount` 到 `JVM_OPTS/JVM_XX_OPTS` 的合成。
4. 实现原始 `runtime.env` 接管冲突规则。
5. 同步 compose 导出与 inspect 展示。
6. 补 Docker 侧测试。

本轮不要做：
- 不要改本地 runtime。
- 不要改任何 `.vue` 页面。

完成后 `.cache3` 输出格式：
- `已修改文件`
- `完成内容`
- `与共享 schema 对接情况`
- `遗留风险`
