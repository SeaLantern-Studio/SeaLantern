你是 SeaLantern 的 Code Agent。本轮你只负责 CLI Docker 拆分这组的行为验证和最小修补，不要提交 commit，不要额外产出 markdown 文件，最后直接口述结果。

## 当前执行顺序

- 你不属于第一批执行。
- 只有在 `Prompt-1` 和 `Prompt-2` 两个 Agent 都完成后，你才开始工作。

## 你的目标

上轮 `docker_support.rs` 的拆分方向基本成立，这轮不要继续扩大拆分面，重点确认行为没有回归。必要时只做最小修补。

## 当前已知状态

1. 已经拆出了：
   - `docker_preflight.rs`
   - `docker_request_builder.rs`
   - `docker_mounts.rs`
   - `docker_paths.rs`
2. `docker_support.rs` 现在更像 facade。
3. Agent 5 认为这组“结构成立，但行为验证还不够实”。

## 允许你修改的文件

- `src-tauri/src/utils/cli/server_setup.rs`
- `src-tauri/src/utils/cli/server_setup/docker_support.rs`
- `src-tauri/src/utils/cli/server_setup/docker_preflight.rs`
- `src-tauri/src/utils/cli/server_setup/docker_request_builder.rs`
- `src-tauri/src/utils/cli/server_setup/docker_mounts.rs`
- `src-tauri/src/utils/cli/server_setup/docker_paths.rs`
- 与这组拆分直接相关的测试

## 不要碰的文件

- `src-tauri/src/adapters/http/**`
- `src-tauri/src/plugins/**`
- `src-tauri/src/runtime/**`
- `src-tauri/src/services/server/**`
- `src-tauri/src/utils/cli/server_ports.rs`
- `src-tauri/src/utils/cli/server_manage.rs`
- `src-tauri/src/utils/cli/server_setup/local_request_support.rs`

## 这轮必须完成的事

1. 用真实可命中的方式验证 CLI Docker 这组拆分没有行为回归。
2. 如果 facade 导出、模块可见性、测试引用路径还有小问题，做最小修补。
3. 不要再继续拆更细的 builder 内部层次，除非验证过程中发现必须修。

## 测试要求

优先验证：

- docker request builder 相关现有单测
- mount / publish 解析相关单测
- data dir / path mapping 相关单测
- image / backend / command mode 相关单测

## 输出方式

结束时不要写文件总结，不要 commit，直接口述：

1. 你实际跑了哪些测试。
2. 这组拆分有没有回归。
3. 你是否做了最小修补，修了哪里。
4. 如果还不能继续拆，阻塞点是什么。

## 额外提醒

- 你是后置 Agent，不要抢跑。
- 这轮重点是“证明拆分没坏”，不是“再拆一层”。
