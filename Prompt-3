你是 Code Agent 3。

这轮任务只补“前端启动概览与预设应用展示”的闭环，不要改 Rust 运行时实现。

调度说明：这是第二阶段任务。请在 `.cache1` 和 `.cache2` 产出后再启动。

唯一输出要求：正常修改代码，并把结果写入 `.cache3`，不要口述回复，不要 commit。

必须遵守：
- 允许编辑前端 API、组合式逻辑、页面组件、语言包。
- 不要改 Rust 启动逻辑。
- 不要使用工程化用语，不要写命令式文案，不要堆多余说明。
- 不要 commit。

必须阅读：
- `.cache1`
- `.cache2`
- `.cache4`
- `src/api/server.ts`
- `src/components/views/create/ServerStartupConfigStep.vue`
- `src/components/config/ConfigStartupSection.vue`
- `src/components/config/useStartupConfigSection.ts`
- `src/views/CreateServerView.vue`
- `src/language/zh-CN.json`

本轮你应该实现的范围：
1. 编辑页优先接入真实启动详情接口，不再只靠本地拼接的近似摘要。
2. 如果建服页暂时拿不到真实后端预览，就把它维持为“当前选择的概览”，但文案不要假装它是最终命令。
3. 把 JVM 预设“已保存 / 已应用 / 关键参数概览”表达清楚。
4. 文案要自然、短，不要出现 `DTO`、`序列化`、`argv` 之类词。
5. 能补的交互校验和空态一起补上。

本轮不要做：
- 不要新增 CPU policy 表单。
- 不要再扩 schema。

完成后 `.cache3` 输出格式：
- `已修改文件`
- `完成内容`
- `启动概览现在分别怎么显示`
- `文案调整`
- `遗留风险`
