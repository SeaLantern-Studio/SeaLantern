[package]
name = "sealantern-server"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"

[lib]
path = "src/lib.rs"

[dependencies]
sealantern-core = { path = "../crates/core" }
tracing = "0.1"
